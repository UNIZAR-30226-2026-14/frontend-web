# Copia este fichero como .env.development (o .env.development.local si no quieres commitear).
# Apunta al backend Spring Boot local cuando corra en un origen distinto al servidor de Vite.
# Ajusta el host/puerto según tu configuración local.
VITE_API_BASE_URL=https://localhost:8443
